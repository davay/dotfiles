[user]
	name = Devin Lim
	email = davay@devinl.im
[credential]
	helper = store
[pull]
	rebase = false
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
